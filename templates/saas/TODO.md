# saas template — placeholder

This template currently mirrors `plain`. The dedicated saas scaffold is
queued. See `../../TEMPLATES.md` for the full lineup and the
`saas` / `content` / `dashboard` / `ai-agent` rows for what each
should ultimately ship.

For now, `npx create-githat-app my-thing --saas` lays down the plain
template — auth + a homepage. Wire your saas-specific routes on top.
